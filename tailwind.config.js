/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./*.html"], // Adjust this to match your file structure
    darkMode: "class", // Ensure dark mode uses the 'dark' class
    theme: {
        extend: {},
    },
    plugins: [],
};
